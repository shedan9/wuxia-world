# 项目协作约定

本仓库当前只有规划文档，尚无游戏工程、可运行 Demo 或已完成的资产。处理任务时先读 [README.md](./README.md)，再按工作范围阅读以下文档：

- [STORY.md](./STORY.md)：三篇连续故事的剧情、任务 ID、人物动机、友情爱情、分支和结局。剧情内容以此为准。
- [ARCHITECTURE.md](./ARCHITECTURE.md)：统一世界、角色约束、系统边界、内容数据与存档契约。系统规则以此为准。
- [DEVELOPMENT_PLAN.md](./DEVELOPMENT_PLAN.md)：M0–M6 交付范围、排期与验收。里程碑承诺以此为准。

## 沟通与本地环境

- 按用户提问语言回答，默认中文；所有输出避免日文假名和日文行文。
- 若后续工具链需要 Node，非交互 shell 先把 fnm 安装的 Node 加入 `PATH`，例如 `export PATH="$HOME/.local/share/fnm/node-versions/v22.22.0/installation/bin:$PATH"`。提交时也要让 hook 找到 Node，不用 `--no-verify` 绕过。
- 界面、美术风格、配色、排版和交互反馈任务，先使用已安装的相关设计 skill，并把当前画风与阶段约束作为输入；用户明确要求跳过时除外。

## 当前交付顺序

1. M0 先做可切换场景和主要 UI 的 Windows x64 视觉 Demo，让用户核对画风、人物比例与探索/战斗视角。样例数据可以驱动画面，不把画面预览写成已完成的玩法。
2. 用户认可视觉方向后，再做 M1–M3 的约 60 分钟玩法 Demo。内容限定为《众路归潮》第一章“江南会客”：一条主线、一条可选支线、三位经典人物直接互动与跨作品机制战；主线所有可达对白分支配音。
3. M4–M8 再制作三篇共 18 章主线、34 条可选支线、成年女性角色恋爱线、最终大战、华山新五绝与三种最终结局。开发进度与实际产能变化应更新开发计划，不提前宣称内容完成。

## 修改内容时守住的边界

- 所有金庸人物同处一个架空年代，但年龄、身份、师承、亲缘和已知经历以各自小说选定的主要剧情阶段为硬约束。新增或修改经典人物前，记录来源作品、参考版本、章节依据、年龄依据、身份及已知事件；核验未完成时标记待核，不编造精确年龄或为了统一年表改龄。
- 原创组织“同路会”、百路簿与归潮石属于本作剧情，不冒充原著设定。归潮石只解释主角的一次穿越，不扩展为常规跨年代旅行系统。
- 主线所需证据必须有保底获取路径。支线、恋爱、可选证人、同行者选择和可选战斗只能改变难度、人物反馈与结局细节，不能使 18 章主线卡死。三篇在同一存档中连续推进，不重置人物成长和世界事实。
- 有姓名、成年且持续互动的女性角色均应设计可达恋爱路径；已有关系须由相关人物自主处理，复数恋人必须逐人知情同意。友情路线同样完整；返现代结局须结算对已有恋人的告别后果。
- 任务、人物、物品、台词和结局使用稳定 ID；修改已使用的 ID 时规划存档迁移。唯一物的转移、角色的事件占用和奖励结算应在任务数据中可追踪，不通过是否播放动画推断事实。
- 对话、任务和角色变更要同步核对 STORY、ARCHITECTURE 与 DEVELOPMENT_PLAN 中受影响的范围、验收和引用。正式配音文本变更还须同步 `line_id` 与音频清单。
- 公开发行的角色、故事、字体、美术、配乐与 AI 音色来源及可用范围需要资产台账确认。当前文档是制作设计，不代表发行授权已落实。

## 验证与报告

文档修改后检查章节、任务数量、ID、内部链接与里程碑范围是否一致。实现任务按开发计划对应阶段执行适当验证；只有实际运行过的构建和测试才能报告为通过。当前没有游戏工程，不能把文档检查称为 Windows 导出或玩法验收。
